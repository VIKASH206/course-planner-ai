/**
 * Polyfills for Mobile and Edge Browser Compatibility
 * This file includes polyfills needed to support older browsers
 */

/**
 * Zone JS is required by Angular itself.
 */
import 'zone.js';  // Included with Angular CLI.

/**
 * APPLICATION IMPORTS
 * Polyfills for features that might not be supported in all browsers
 */

// Add global to window for better compatibility
(window as any).global = window;
