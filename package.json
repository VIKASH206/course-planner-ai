{
  "name": "course-planner-ai",
  "version": "1.0.0",
  "description": "Course Planner AI - Monorepo",
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
